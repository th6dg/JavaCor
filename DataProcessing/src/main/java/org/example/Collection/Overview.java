package org.example.Collection;

/**
 *  @Interface:
 *      Collection: add(), remove(), size(), iterator(),...
 *      List: Tập hợp có thứ tự
 *      Queue: List + FIFO
 *      Set: Không cho phép trùng lặp
 *      Map: Key-Value
 *
 *  @Class:
 *      ArrayList / LinkedList
 *      HashSet / LinkedHashSet / TreeSet
 *      HashMap / LinkedHashMap / TreeMap
 *
 *  @Algorithm:
 *      sort()
 *      binarySearch()
 *      shuffle()
 *      reverse()
 *
 *  @Generic:
 *
 *  @CompareInterface:
 *      List: Control thứ tự, CopyValue
 *      Set: Uncontrol thứ tự, unique
 *      Map: Uncontrol thứ tự, (unique)Key - Value
 *
 *  @Iterator
 *  @ListIterator
 */

public class Overview {
}
