package org.example.Collection.Set;

/**
 *  @Overview: Binary Tree + Set
 *
 *  @Feature:
 *      Sắp xếp tự động
 *      Không cho phép trùng lặp
 *      Crud O(logn)
 *
 *  @Method:
 *      add(e), remove(o)
 *      first(), last(), higher(e), lower(e),
 *      ceiling(e), floor(e), subSet(f,t), headSet(t), tailSet(f),...
 *
 *  @Ưu/Nhược:
 *      Thứ tự sorted
 *      ... < O(logn) < O(1)
 *
 *  @UseCase:
 *      Khi cần based-sorted structure
 */

public class TreeSet1 {
}
